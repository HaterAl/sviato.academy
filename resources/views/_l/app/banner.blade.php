<header class="flex mx-auto max-w-7xl px-2 py-6 sm:px-6 lg:px-8 bg-gray-900">
    <a
        href="#"
        title="{{ $config->name }}"
    >
        <img
            src="{{ Vite::asset('resources/images/app/ideil.svg') }}"
            width="72"
            height="24"
            alt="{{ $config->name }}"
        >
    </a>

    <button type="button" aria-label="Навігація" class="text-gray-300">Меню</button>
</header>
